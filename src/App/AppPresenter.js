import React from 'react';
import Routes from '../routes';

const AppPresenter = (porps) => {
  /* Router */
  /* State */
  /* Hooks */
  /* Functions */
  /* Render */
  return <Routes />;
};

export default AppPresenter;
