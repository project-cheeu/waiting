import React from 'react';

const VideoPresenter = (props) => {
  /* Router */
  /* State */
  /* Hooks */
  /* Functions */
  /* Render */
  return <div>Video</div>;
};

export default VideoPresenter;
